from dotenv import load_dotenv
import os
load_dotenv()


from google import genai
from PIL import Image
from io import BytesIO

import warnings
from urllib3.exceptions import NotOpenSSLWarning

warnings.filterwarnings("ignore", category=NotOpenSSLWarning)

api_key = os.getenv("GEMINI_API_KEY")

client = genai.Client(api_key=api_key)

prompt = """ Make the person into a bald, white mannequin, focusing on the hands especially if they are holding objects or wearing long sleeves, thighs and legs. Absolutely keep all clothes."""

image = Image.open("/Users/sehyunk/Desktop/Ariel.png")
# Call the API to generate content
response = client.models.generate_content(
    model="gemini-2.5-flash-image-preview",
    contents=[prompt, image]
)

# The response can contain both text and image data.
# Iterate through the parts to find and save the image.
for part in response.candidates[0].content.parts:
    if part.text is not None:
        print("First part; No valid response from Gemini — possibly safety-blocked or misconfigured.")
    elif part.inline_data is not None:
        image = Image.open(BytesIO(part.inline_data.data))
        image.save("mannequin.png")




prompt = """Take the reference image and extract the dominant color from the face area. Repaint the entire mannequin surface using this color. Keep lighting, shadows, and subtle facial shading consistent. Make sure to repaint the white sections near the image's corners as well. """
image1 = Image.open("/Users/sehyunk/Desktop/face2.png")

response = client.models.generate_content(
    model="gemini-2.5-flash-image-preview",
    contents=[prompt, image, image1]
    
)

for part in response.candidates[0].content.parts:
    if part.text is not None:
        print("Middle part; No valid response from Gemini — possibly safety-blocked or misconfigured.")
    elif part.inline_data is not None:
        image = Image.open(BytesIO(part.inline_data.data))
        image.save("color.png")


GPT_Generated_Description = "Leans back slightly on her left arm while sitting at the edge of a pool, with her left leg bent and right leg extended out of frame. Holds a clear plastic cup filled with a bright blue drink in her right hand, sipping through a white straw. Tilts her head subtly to the right, giving a soft, composed expression. Gazes downward and to her left, avoiding eye contact with the camera or viewer. Slightly parts her lips around the straw as she drinks. Wears a two-piece outfit that includes a halter top and high-waisted bottom with a decorative element on the waistband. Keeps her posture relaxed, creating a casual and effortless vibe. Appears to be illuminated by warm, natural sunlight, enhancing the poolside atmosphere."
prompt = f"""Have this person be in the empty background while matching her body on the outline and wearing the clothes given. and wearing the clothes given. She should be {GPT_Generated_Description}, Absolutely do not change hairstyle, especially if long. Absolutely do not change background. """


response = client.models.generate_content(
    model="gemini-2.5-flash-image-preview",
    contents=[prompt, image, image1]
)
# The response can contain both text and image data.
# Iterate through the parts to find and save the image.
for part in response.candidates[0].content.parts:
    if part.text is not None:
        
        print("Final part; No valid response from Gemini — possibly safety-blocked or misconfigured.")
    elif part.inline_data is not None:
        image = Image.open(BytesIO(part.inline_data.data))
        image.save("finished.png")
        print("FINISHED WITH GENERATION")